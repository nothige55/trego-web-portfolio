# Chat

프로젝트 채팅의 메시지 계약과 실시간 동작을 관리합니다.

- SignalR 연결 자체는 `lib`의 공용 client를 주입받습니다.
- `SendMessage`는 서버가 ID를 발급하므로 메시지를 낙관적으로 추가하지 않습니다.
- 초기 진입과 재연결에서는 REST 이력을 교체하고, 실시간 이벤트는 `messageId`로 병합합니다.
- 전송 실패 시 원문 draft를 결과와 callback에 보존하며 자동 재시도하지 않습니다.
- AI 응답과 thinking 이벤트는 이 범위에 포함하지 않습니다.
